# Investment Operations Dashboard — Dependencies
# Minimum-version bounds — compatible with Python 3.12 through 3.14.
# Hard pins removed so pip resolves pre-built wheels for your Python version.
# Install with: pip install -r requirements.txt

# Core data handling
pandas>=2.2.0
numpy>=1.26.4

# Live market data — primary source
yfinance>=0.2.37

# Database
# sqlite3 is included in Python standard library — no install needed

# Environment variable management
# Keeps API keys out of the codebase entirely
python-dotenv>=1.0.1

# Chart generation
matplotlib>=3.8.3
mplfinance>=0.12.10b0
seaborn>=0.13.2

# Market calendar — exchange-specific holiday logic
# Used by reconciliation.py for correct trading day exclusions
pandas-market-calendars>=4.3.1

# Testing
pytest>=8.1.1
